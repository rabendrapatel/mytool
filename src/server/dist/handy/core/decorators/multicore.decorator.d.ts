export declare const MultiCore: (settings?: {
    internalPropName?: string;
    globalChangeEventName?: string;
    consoleSettings?: boolean;
}) => (target: any, name: any, descriptor?: PropertyDescriptor) => any;
